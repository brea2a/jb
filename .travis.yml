os:
 - linux
 - osx
sudo: false
language: c
compiler:
 - gcc
 - clang
before_install:
 - echo $TRAVIS_OS_NAME
 - uname -a
 - echo $LANG
 - echo $LC_ALL
before_script:
 - autoreconf -i
 - ./configure
script:
 - make
 - make check

